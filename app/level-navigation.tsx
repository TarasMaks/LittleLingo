'use client';
import {levelCatalog,levelInfo,normalizeLevel} from '@/lib/curriculum';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
export default function LevelNavigator({value,current,onChange,onManage}:{value:string;current:string;onChange:(value:string)=>void;onManage:()=>void}){const info=levelInfo(value);return <section className="level-browser" aria-label="Learning levels"><div><label htmlFor="browse-level">Browse practice level</label><Select value={normalizeLevel(value)} onValueChange={onChange}><SelectTrigger id="browse-level" aria-label="Browse practice level"><SelectValue/></SelectTrigger><SelectContent>{levelCatalog.map(l=><SelectItem key={l.id} value={l.id}>{l.id} · {l.name}</SelectItem>)}</SelectContent></Select><p>{info.description}</p></div><div className="level-browser-note"><span className="tag">Saved path: {normalizeLevel(current)}</span><p>Explore any level. An adult chooses your saved learning path.</p><button className="text-link" onClick={onManage}>Manage practice level</button></div></section>}
